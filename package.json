{
  "name": "todo-app-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-web": "npm run dev --workspace todo-web",
    "start-server": "npm run start --workspace todo-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "todo-server",
    "todo-web"
  ]
}
